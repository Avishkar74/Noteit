# Snabby Backend – Environment Variables
# Copy this to .env for local development, or set in Railway dashboard.

# Port (Railway sets this automatically)
PORT=3000

# Public base URL (your Railway deployment URL)
# Railway auto-assigns this, but you can set it explicitly
BASE_URL=https://YOUR_RAILWAY_URL.up.railway.app

# Node environment
NODE_ENV=production

# Session data directory (Railway uses ephemeral filesystem by default;
# attach a volume at /data for persistence)
SESSION_DATA_DIR=/data/sessions
